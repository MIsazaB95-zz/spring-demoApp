apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-demo
  namespace: default
  labels:
    app: spring-demo
spec:
  replicas: 3
  minReadySeconds: 45
  selector:
    matchLabels:
      app: spring-demo
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: spring-demo
    spec:
      containers:
        - image: 486912667928.dkr.ecr.us-east-1.amazonaws.com/spring-demo:latest
          imagePullPolicy: IfNotPresent
          name: spring-demo
          ports:
            - containerPort: 8081
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 125m
              memory: 120Mi
      restartPolicy: Always
      terminationGracePeriodSeconds: 30